.DS_Store

# KiCad transient / generated
*-backups/
*.kicad_prl
fp-info-cache
*.lck
*.bak
*.kicad_sch-bak
*.kicad_pcb-bak
_autosave-*
*.net

# Build outputs (regenerate from source; keep only tagged release zips)
hardware/build/

# Gerber working copy (regenerate from the zip in fab/)
fab/v1.5-easyeda/gerber-extracted/

# KiCad 10 local history (its own nested git repo)
.history/
